{
  "private": true,
  "name": "mono",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "devest test",
    "test:ci": "devest test --runInBand",
    "test:all": "lerna run test --parallel --since",
    "lint": "devest lint --onlyChanged",
    "type:gen": "lerna run type:gen",
    "typecheck:slow": "jest -c jest-typecheck.config.js --onlyChanged",
    "tscheck": "lerna run --parallel type:check",
    "dev:test": "jest --watch --bail",
    "dev:lint": "yarn run lint --watch",
    "dev:typecheck:slow": "yarn run typecheck --watch",
    "build": "lerna exec --parallel 'BABEL_ENV=build yarn build'",
    "postbuild": "yarn run type:gen && yarn run format",
    "commit": "git add -A && git commit -sS",
    "format": "prettier '**/*' --write",
    "release": "echo fake-for-now",
    "releaseXX": "lerna publish -m 'chore: release [skip ci]' --conventional-commits --changelog --sign-git-commit --sign-git-tag",
    "import": "lerna import -y",
    "postimport": "rm -f {@tunnckocore,packages}/*/{.{git,es,ed,all-,pret,nyc}*,*.md,LICENSE,*.js}"
  },
  "dependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "@babel/preset-react": "^7.0.0",
    "@babel/preset-typescript": "^7.1.0",
    "@tunnckocore/devest": "^0.4.0",
    "@types/jest": "^23.3.13",
    "@types/node": "^10.12.18",
    "babel-plugin-module-resolver": "^3.1.3",
    "eslint": "^5.12.0",
    "eslint-config-tunnckocore": "^3.0.0",
    "eslint-import-resolver-babel-module": "^5.0.0",
    "jest": "^24.0.0",
    "jest-runner-eslint": "^0.7.3",
    "jest-runner-tsc": "^1.3.2",
    "lerna": "^3.10.5",
    "prettier": "^1.15.3",
    "typescript": "^3.2.2"
  },
  "jest-runner-eslint": {
    "cliOptions": {
      "format": "codeframe",
      "quiet": true,
      "cache": true,
      "fix": true
    }
  },
  "browserslist": [
    "node 8",
    "node 10"
  ],
  "workspaces": [
    "packages/*",
    "@tunnckocore/*"
  ],
  "extensions": [
    "ts",
    "tsx",
    "js",
    "jsx",
    "mjs"
  ]
}
