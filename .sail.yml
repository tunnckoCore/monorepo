tasks:
  build:
    image: node:10
    command:
      - yarn
    args:
      - install
  lint:
    image: node:10
    command:
      - yarn
    args:
      - lint
  test:
    image: node:10
    command:
      - yarn
    args:
      - test
      - --coverage
